plugins {
    base
}

description = "Columnar — high-performance in-memory column-oriented table framework with lazy viewport-driven evaluation, tiered on/off-heap storage, and runtime bytecode codegen."

allprojects {
    group = "io.columnar"
    version = "0.1.0-SNAPSHOT"
}
